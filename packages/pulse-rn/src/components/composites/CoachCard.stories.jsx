import React from 'react';
import CoachCard from './CoachCard';

export default { title: 'Composites/CoachCard', component: CoachCard };

export const Default = () => <CoachCard message="Your appetite suppression is stable. Try a 10-minute walk after lunch." />;
